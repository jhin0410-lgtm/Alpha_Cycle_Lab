# Contributing

Python 3.12 환경에서 `python -m pip install -e ".[dev]"`로 설치하십시오. 변경 전후에
`python -m ruff check .`, `python -m mypy`, `python -m pytest`를 모두 실행합니다.
테스트는 외부 네트워크 없이 결정론적으로 동작해야 하며 실제 키·계좌·개인 데이터나
성과 주장을 포함하면 안 됩니다.

전략은 목표 포지션만 반환해야 하며 브로커를 호출해서는 안 됩니다. 새로운 데이터는 공개
시점 계약과 미래 데이터 비노출 테스트를 포함해야 합니다. 금융 가정이나 비용 기본값을
바꾸면 문서와 회귀 테스트를 함께 갱신하십시오.

